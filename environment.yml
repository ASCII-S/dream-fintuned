name: dream-sft
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip>=23.0
  
  # 通过pip安装的包
  - pip:
    - torch==2.5.1
    - transformers==4.46.2
    - datasets>=2.14.0
    - pandas>=2.0.0
    - numpy>=1.24.0
    - pyarrow>=12.0.0
    - accelerate>=0.30.0
    - sentencepiece>=0.1.99
    - protobuf>=3.20.0
    - wandb>=0.15.0
    - tensorboard>=2.13.0
    - tqdm>=4.65.0
    - jupyter>=1.0.0
    - matplotlib>=3.7.0
    - seaborn>=0.12.0
    - jsonlines>=3.1.0
    - einops>=0.6.1
    - omegaconf>=2.3.0
    - hydra-core>=1.3.0 